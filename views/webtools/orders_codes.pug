<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/css/ocvt.css">
  <title>Unredeemed Codes!</title>
</head> 
<body class="webtools">
  <a href="/webtools">&lt;-- Back to Webtools Home</a>

  <h2>All Unredeemed Store Codes</h2>

  <table cellpadding="2" cellspacing="0">
    <tr>
      <th>Generated Date&nbsp;</th>
      <th>Generated By&nbsp;</th>
      <th>Store Item&nbsp;</th>
      <th>Item Count&nbsp;</th>
      <th>Item Cost&nbsp;</th>
      <th>Total Cost&nbsp;</th>
      <th>Code<br>(multiple items in same order share the same code)&nbsp;</th>
      <th>Notes</th>

    - let i = 0
    each code in codes
      if i % 2 === 0
        <tr bgcolor="#CCCCCC">
      else
        <tr>
      - i += 1
      
      <td>#{code.createDatetime}</td>
      <td>#{code.generatedByName}</td>
      <td>#{code.storeItemId}</td>
      <td>#{code.storeItemCount}</td>
      <td>#{code.amount}</td>
      <td>#{code.storeItemCount * code.amount}</td>
      <td>#{code.code}</td>
      <td>#{code.note}</td>
      </tr>
  </table>
</body>
