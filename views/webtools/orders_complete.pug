<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/css/ocvt.css">
  <script src="/js/ocvt_webtools.js"></script>
  <title>Complete Orders!</title>
</head> 
<body class="webtools">
  <a href="/webtools">&lt;-- Back to Webtools Home</a>

  <h2>#{header}</h2>

  <table cellpadding="2" cellspacing="0">
    <tr>
      <th>Order Date&nbsp;</th>
      <th>Member #&nbsp;</th>
      <th>Member Name&nbsp;</th>
      <th>Email&nbsp;</th>
      <th>Store Item&nbsp;</th>
      <th>Item Count&nbsp;</th>
      <th>Item Cost&nbsp;</th>
      <th>Total Cost&nbsp;</th>
      <th>Notes&nbsp;</th>
      <th>Payment Id<br>(multiple items in same payment share the same id)&nbsp;</th>
      <th>Payment Method&nbsp;</th>
      <th>Member who entered order</th>
    </tr>

    - let i = 0
    each order in orders
      if i % 2 === 0
        <tr bgcolor="#CCCCCC">
      else
        <tr>
      - i += 1

      <td>#{order.createDatetime}</td>
      <td>#{order.memberId}</td>
      <td>#{order.memberName}</td>
      <td>#{order.email}</td>
      <td>#{order.storeItemId}</td>
      <td>#{order.storeItemCount}</td>
      <td>#{order.amount}</td>
      <td>#{order.storeItemCount * order.amount}</td>
      <td>#{order.note}</td>
      <td>#{order.paymentId}</td>
      <td>#{order.paymentMethod}</td>
      <td>#{order.enteredByName}</td>
      </tr>
  </table>
</body>
